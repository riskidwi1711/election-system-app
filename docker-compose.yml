version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: laravel-app
    container_name: laravel-app
    restart: unless-stopped
    ports:
      - "8080:80"
    networks:
      - laravel-net

networks:
  laravel-net:
    driver: bridge
